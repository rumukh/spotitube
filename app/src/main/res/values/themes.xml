<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Spotitube" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- LinkHandlerActivity floats over whatever the user was doing; Compose draws the card. -->
    <style name="Theme.Spotitube.Overlay" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>
</resources>